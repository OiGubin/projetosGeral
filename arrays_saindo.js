// Montando o garda-roupa

let guardaRoupa = ["Camisa thrasher", "calça jeans cargo preto", "tênis nike jordan preto com detalhes roxos", "colar prata"]

// Exibindo as opções do guarda-roupa

console.log("Meu guarda roupa tem: ", 
guardaRoupa)

    // Selecionando um look para sair

    let lookSelecionado = [] // look vazio

    // usando push para adicionar peças de roupas do nosso guarda-roupa no nosso look

    lookSelecionado.push(guardaRoupa[0])  // adiciona a peça em 0
    lookSelecionado.push(guardaRoupa[1])  // adiciona a peça em 1
    lookSelecionado.push(guardaRoupa[2])  // adiciona a peça em 2
    lookSelecionado.push(guardaRoupa[3])  // adiciona a peça em 3


    // Exibindo o look selecionado

console.log("seu look para sair é: ", lookSelecionado)

